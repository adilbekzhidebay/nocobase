apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nocobase.fullname" . }}-test-connection"
  labels:
    {{- include "nocobase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nocobase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
